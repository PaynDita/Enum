find / -perm -u=s -type f 2>/dev/null

find / -name fileName 2>/dev/null

eg.
find / -name php-reverse-shell.php 2>/dev/null

You can use "locate" command too.

-----------------------------------------------------------------

In Windows:

dir /s /b C:\*filename*   -- (cmd.exe)


Get-ChildItem -Path C:\ -Recurse -Filter *root.txt*   (powershell)
OR Get-ChildItem -Path C:\ -Recurse -Filter *filename* | Select-Object FullName  (PowerShell)
 
