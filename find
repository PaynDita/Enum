find / -perm -u=s -type f 2>/dev/null

find / -name fileName 2>/dev/null

eg.
find / -name php-reverse-shell.php 2>/dev/null

You can use "locate" command too.


dir /s /b C:\*filename*   --In Windows
